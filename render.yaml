services:
  - type: web
    name: misstech-backend
    runtime: node
    rootDir: Backend
    buildCommand: npm install
    startCommand: node index.js
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: MONGO_URL
        sync: false
    autoDeploy: true
    healthCheckPath: /
