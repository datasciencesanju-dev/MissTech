{
  "name": "misstech-root",
  "version": "1.0.0",
  "private": true,
  "description": "MissTech Full Stack Project - Frontend & Backend",
  "scripts": {
    "dev": "concurrently -n \"FRONTEND,BACKEND\" -c \"cyan,magenta\" \"npm run dev --prefix Frontend\" \"npm run dev --prefix Backend\"",
    "start": "concurrently -n \"FRONTEND,BACKEND\" -c \"cyan,magenta\" \"npm run dev --prefix Frontend\" \"npm run start --prefix Backend\"",
    "frontend": "npm run dev --prefix Frontend",
    "backend": "npm run dev --prefix Backend",
    "build": "npm run build --prefix Frontend",
    "install:all": "npm install && npm install --prefix Frontend && npm install --prefix Backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
